%p Welcome to my new app :)

- @articles.each do |article|
	= link_to (image_tag article.image.url(:thumb)), article
	%h2= link_to article.title, article
	%p
		Shared by
		= article.user.name
		about
		= time_ago_in_words(article.created_at)

